#include<stdio.h>
#include<conio.h>

void main()
{
    int num;

    printf("Enter one number: ");
    scanf("%d",&num);

    switch(num)
    {
        case if(num % 2 == 0):

        printf("%d is an even number",num);
        break;

        default:
        
        printf("%d is an odd number");
        break;
    }
}